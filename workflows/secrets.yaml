# CRE workflow secrets
# For simulation: values loaded from .env. Secret id → env var name.
# See https://docs.chain.link/cre/guides/workflow/secrets

secretsNames:
  CRE_ETH_PRIVATE_KEY:
    - ETH_PRIVATE_KEY
